[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: Unit tests (no network, no I/O beyond temp files)
    integration: Integration tests (may require database or file system)
    slow: Slow tests (require real LLM API calls or heavy computation)
