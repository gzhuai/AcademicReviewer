# 学术竞赛文稿审查智能体 —— 完整设计方案 v1.4

---

## 一、核心定位与设计原则

### 1.1 核心定位

这个系统的本质是一个 **"AI 学术导师 + 质量门禁"**，而非代写工具。它的核心价值在于：

> 在不替代学生思考的前提下，系统性地发现文稿中的问题，并以**可操作的反馈**指导学生自主改进。

### 1.2 设计原则（六项铁律）

| 原则 | 说明 |
|------|------|
| **学生主权** | AI 只做批注和建议，绝不直接改写正文，确保作品仍为学生原创 |
| **分层审查** | 从宏观（论点结构）到微观（语法拼写），逐层把关，避免挂一漏万 |
| **竞赛对齐** | 每次审查都与具体竞赛的评分标准（rubric）严格对标 |
| **可解释性** | 每个建议必须附带理由和评分维度，让学生"知其所以然" |
| **迭代闭环** | 支持多轮提交→审查→修改→再审，跟踪质量收敛趋势 |
| **数据驱动校准** | 用获奖/失败样本持续反推评委隐性偏好，校准配置文件（v1.3 新增） |

---

## 二、系统架构：双管线协作体系

### 架构全景图

v1.3 的核心架构演进：将系统分为两条独立的管线——**审查管线**（在线，实时审查学生文稿）和**校准管线**（离线，赛季后批量分析样本、更新配置）。两条管线通过**配置文件层**解耦。

```
┌─────────────────────────────────────────────────────────────────┐
│                        AcademicReviewer                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────┐          ┌──────────────────────┐    │
│  │   审查管线（在线）      │          │   校准管线（离线）      │    │
│  │                      │          │                      │    │
│  │  学生文稿 ─→ Orchestrator ─→   │   获奖/失败文章集 ─→     │    │
│  │                │       │       │   Calibration Engine   │    │
│  │    ┌───┬───┬───┼───┬───┤       │         │              │    │
│  │    ▼   ▼   ▼   ▼   ▼   ▼       │    ┌─────▼─────┐       │    │
│  │   A1  A2  A3  A4  A5  A6  A7   │    │ 特征提取   │       │    │
│  │    │   │   │   │   │   │   │    │    │ 差异分析   │       │    │
│  │    └───┴───┴───┴───┴───┴───┘    │    │ 规则生成   │       │    │
│  │             │                  │    │ Diff 输出  │       │    │
│  │             ▼                  │    └─────┬─────┘       │    │
│  │       审查报告                  │          │              │    │
│  └──────────────┬───────────────┘          │              │    │
│                 │                          ▼              │    │
│                 │              ┌──────────────────┐       │    │
│                 │   读取配置    │    配置文件层      │ ◀写入  │    │
│                 └─────────────▶│  · rubric JSON    │───────┘    │
│                               │  · structure schema│          │
│                               │  · evidence patterns│         │
│                               │  · style guide     │          │
│                               │  · citation rules  │          │
│                               └──────────────────┘          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**关键解耦**：配置文件是两条管线之间的唯一数据契约。审查管线不关心配置是怎么来的；校准管线不关心配置被谁消费。

---

### 2.1 审查管线：七个专业 Agent

采用 **编排器 + 专家智能体池（Orchestrator + Agent Pool）** 架构。每个 Agent 从配置文件层读取对应配置，执行单篇文稿的专项审查。

```
┌──────────────────────────────────────────────────────────┐
│                     Orchestrator                         │
│              (任务调度 / 结果聚合 / 质量报告)               │
└──────┬───────┬───────┬───────┬───────┬───────┬──────────┘
       │       │       │       │       │       │
   ┌───▼──┐┌──▼───┐┌──▼───┐┌──▼───┐┌──▼───┐┌──▼──────┐
   │Rubric││Logic ││Argue ││Style ││Citation││Original│
   │Parser││&Flow ││&Evid ││&Voice││Checker││  Check  │
   └──────┘└──────┘└──────┘└──────┘└───────┘└─────────┘
```

#### Agent 1: Rubric Parser（评分标准解析器）
- **输入**：竞赛官方 rubric（PDF/图片/文本）
- **输出**：结构化的评分维度树 + 权重 + 关键词
- **能力**：将模糊的评分描述（如 "demonstrates critical thinking"）转化为可检查的具体指标
- **配置来源**：`rubric_calibrated.json`（由 Calibration Engine 生成）

#### Agent 2: Structure & Logic（结构与逻辑审查）
- 检查论文整体骨架：引言→文献综述→方法→论证→结论的完整性
- 检测段落间的逻辑衔接（是否存在跳跃、重复、缺失环节）
- 评估 thesis statement 的清晰度和全文一致性
- **配置来源**：`structure_schema_calibrated.json`

#### Agent 3: Argument & Evidence（论点与证据审查）
- 核查每个 claim 是否有充分 evidence 支撑
- 识别逻辑谬误（因果倒置、稻草人、以偏概全等）
- 评估证据来源的权威性和时效性
- 依据校准后的 fatal_defects 标记「有则必死」的硬伤
- **配置来源**：`evidence_patterns.json`（按赛事类型）

#### Agent 4: Writing Style & Voice（写作风格与语气）
- 检查写作风格规范性（避免口语化、情绪化表达）
- 术语使用的一致性
- 被动语态/主动语态的合理分布
- 是否符合目标竞赛的预期风格
- **配置来源**：`style_guide.json`（按赛事类型）

#### Agent 5: Citation & Format（引用与格式审查）
- 检查所有引用是否完整（有 cite 必有 reference，反之亦然）
- 验证引用格式（APA / MLA / Chicago 等）的一致性
- 检测可疑的 AI 生成引用（虚假文献检测）
- **配置来源**：`citation_rules.json`

#### Agent 6: Originality Guardian（原创性守护）
- 检测潜在抄袭/过度借鉴段落（与内部历史稿件库比对）
- 识别大段 AI 生成痕迹（语气突变、模板化结构）
- 确保学生个人 voice 得以保留
- **配置**：无需配置（L0 通用引擎）

#### Agent 7: Language Polishing（语言精修）—— 唯一允许改写的 Agent
- 语法、拼写、标点修正
- 中式英语 → 地道表达的建议
- 长难句拆分建议
- **严格以批注模式工作，不直接改动原文**
- **配置**：仅 L1 风格开关（学术模式 / 商务模式）

---

### 2.2 配置层：Agent 与配置文件的映射

配置文件是审查管线和校准管线的数据契约。它在 v1.3 中的角色从「静态模板」升级为「持续校准的活文档」。

| 配置文件 | 消费方 | 生成/更新方 | 校准频率 |
|----------|--------|------------|----------|
| `rubric_calibrated.json` | Agent 1 | Calibration Engine | 每赛季校准 |
| `structure_schema_calibrated.json` | Agent 2 | Calibration Engine | 每赛季校准 |
| `evidence_patterns.json` | Agent 3 | Calibration Engine | 每次有新样本 |
| `style_guide.json` | Agent 4 | Calibration Engine | 每赛季或按需 |
| `citation_rules.json` | Agent 5 | 手动定义 | 无需校准 |
| 无（通用引擎） | Agent 6, 7 | 无 | 无需配置 |

```
配置文件示例（evidence_patterns.json，科研型）:

{
  "type": "research",
  "season": "2026",
  "evidence_standards": {
    "quantitative": {
      "expect_p_value": true,
      "p_value_threshold": 0.05,
      "expect_effect_size": true,
      "expect_sensitivity_analysis": true
    },
    "source_quality": {
      "min_primary_sources": 12
    }
  },
  "fatal_defects": [
    { "defect": "no_control_group", "loser_rate": 35, "winner_rate": 2 },
    { "defect": "citation_appears_fabricated", "loser_rate": 18, "winner_rate": 1 }
  ],
  "confidence": 0.82,
  "based_on": "15_winners + 12_losers + 5_external_winners"
}
```

---

### 2.3 校准管线：Calibration Engine

#### 2.3.1 定位

Calibration Engine 是一个**纯统计管线**，不调用 LLM。它在赛季后以批量离线模式运行，从获奖/失败文章的差异中提取可量化的校准信号，输出更新后的配置文件。

#### 2.3.2 核心方法论：Cohen's d 差异分析

不凭感觉说「获奖文章好像更重视 X」，而是用效应量（effect size）量化：

```
d = (M_winners - M_losers) / SD_pooled

d > 0.8  → 大效应 — 决定性差异 → 写入配置文件 critical 项
d > 0.5  → 中效应 — 重要差异 → 写入 major 项
d > 0.2  → 小效应 — 轻微差异 → 写入 minor 项
d < 0.2  → 无差异 — 评委可能不关心 → 不写入
```

#### 2.3.3 四阶段处理流程

```
┌──────────────────────────────────────────────────────────┐
│              Calibration Engine 四阶段流程                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Step 1: 特征提取（全自动，离线）                          │
│  ├── 对获奖/失败/外部获奖文章逐篇结构化特征扫描               │
│  ├── 特征维度：句式参数、引用密度、段落骨架覆盖度、            │
│  │           论证密度、被动语态占比、词汇多样性、              │
│  │           逻辑标志词密度、过渡句频率、证据多样性...         │
│  └── 输出：特征矩阵（篇 × 特征）                            │
│                                                          │
│  Step 2: 分组差异分析                                     │
│  ├── 分组：我方获奖 / 我方失败 / 外部获奖                    │
│  ├── 对每个特征 × 每组对，计算 Cohen's d                    │
│  ├── 交叉验证：我方获奖 ∩ 外部获奖 = 确定性信号              │
│  │            外部获奖独有 = 我方辅导盲区                   │
│  └── 输出：效应量排名表 + 置信区间                          │
│                                                          │
│  Step 3: 规则生成                                         │
│  ├── d > 0.8 → 写入 critical 项                           │
│  ├── d > 0.5 → 写入 major 项                              │
│  ├── 失败文章独有而获奖文章几乎不出现 → fatal_defects       │
│  ├── 为每条规则标注 confidence + data_source               │
│  └── 输出：新版本配置文件（JSON）                           │
│                                                          │
│  Step 4: Diff 生成（面向运营人员）                          │
│  ├── 与上一赛季配置文件对比                                  │
│  ├── 标注每条变更的类型：新增 / 参数调整 / 权重修正            │
│  ├── 附带变更理由（效应量 + 数据来源）                       │
│  └── 输出：human-friendly 校准建议报告                      │
│                                                          │
│  备注：全程不调用 LLM。这是统计 + 规则，不是 AI。            │
└──────────────────────────────────────────────────────────┘
```

#### 2.3.4 三类校准信号

| 信号类型 | 来源 | 示例 | 写入配置文件位置 |
|----------|------|------|-----------------|
| **正向信号** | 获奖文章共同特征 | "获奖者 Methods 段都有可复现性声明 (d=0.78)" | `quality_expectations` |
| **负向信号** | 失败文章独有缺陷 | "失败者 35% 缺少控制组，获奖者仅 2%" | `fatal_defects` |
| **交叉验证信号** | 我方获奖 ∩ 外部获奖 | "gap statement 使用率双方均 > 80%" → 高置信 | `confidence: high` |

---

### 2.4 各 Agent 校准需求矩阵

并非所有 Agent 都需要校准。校准深度由「评委对该维度是否存在隐性偏好」决定。

| Agent | 需要校准？ | 校准深度 | 校准方式 | 校准对象 | 校准频率 |
|-------|:---:|----------|----------|----------|----------|
| **A1 Rubric** | ✅ | 深 | 维度权重反推、子维度发现 | `rubric_calibrated.json` | 每赛季 1 次 |
| **A2 Structure** | ✅ | 中浅 | 二元检查项升级为「有+质量预期」 | `structure_schema_calibrated.json` | 每赛季 1 次 |
| **A3 Argument** | ✅✅ 强需 | 最深 | 论证模式反推、fatal_defects 挖掘 | `evidence_patterns.json` | 有新样本即触发 |
| **A4 Style** | ✅ | 中 | 风格特征区间统计 | `style_guide.json` | 每赛季或按需 |
| **A5 Citation** | ❌ | — | 规则驱动（APA/MLA等），无隐性偏好 | — | — |
| **A6 Originality** | ❌ | — | L0 通用引擎 | — | — |
| **A7 Language** | ❌ | — | L0 通用，仅风格开关为 L1 | — | — |

**解读：**

- **A3 是校准的重中之重**。不同赛事类型对「好论证」的定义有隐性标准（如 ISEF 获奖者用 p-value + effect size 双重报告，而官方 rubric 只写 "demonstrates critical thinking"），只有样本能揭示。
- **A1 校准的是「评分标准」**（评委声称看重什么），**A3 校准的是「评分实质」**（评委实际看重什么）。两者缺一不可。
- **A2/A4 可放在第二阶段做**，不影响 MVP。先用启发式规则运行，有足够样本后再做定量校准。

---

### 2.5 竞赛类型五分类

系统将常见国际竞赛归为五大类型，每种类型驱动不同的 L2 审查逻辑（Agent 3 + Agent 4）：

| 类型 | Agent 3 审查重点 | Agent 4 风格标准 | 典型竞赛 |
|------|-----------------|-----------------|----------|
| **科研型** | 实验设计、数据显著性、可复现性、变量控制 | 学术-技术：干练精确、被动语态可接受 | ISEF, Regeneron STS, ISSDC |
| **思辨型** | 逻辑链完整性、哲学引用深度、反驳力度 | 学术-思辨：优雅有力、展示个人 voice | John Locke, Marshall Society |
| **社科型** | 一手调研方法论、案例分析深度、跨文化视角 | 学术-社科：调研叙事、平衡客观与洞察 | CTB, YOC, YIC |
| **历史型** | 一手史料引用、史学视角、contextualization | 学术-史学：史实精准、时代语境还原 | NHD, The Concord Review |
| **金融型** | 估值模型合理性、财务数据溯源、风险分析框架、图表质量 | 商务-金融：简洁果断、数据驱动、主动语态 | SIC, WGHS, YIS |

**金融型竞赛的特殊性：**

| 维度 | 传统学术论文 | 金融投资报告 |
|------|-------------|-------------|
| **写作目的** | 论证学术观点 | 说服投资决策 |
| **核心骨架** | Intro → Lit Review → Method → Results → Discussion | Executive Summary → Investment Thesis → Industry Analysis → Valuation → Portfolio → Risk Analysis |
| **证据类型** | 学术引用、实验数据、一手史料 | 财务数据、估值模型（DCF/Comparables）、SEC 文件、Bloomberg 数据 |
| **写作风格** | 学术英语，被动语态可接受，hedging language | 商务英语，短句优先（15-20词），主动语态，明确判断 + 证据支撑 |
| **图表角色** | 辅助说明 | **核心叙事载体**（K线图、财务模型、敏感性分析表） |
| **引用体系** | APA/MLA/Chicago 正式引用 | 脚注式数据来源标注，精确但不拘泥于格式 |
| **关键风险** | 逻辑谬误、证据不足 | 估值假设不合理、数据来源缺失、风险对冲策略缺失 |

---

### 2.6 精简方案与取舍分析

#### 2.6.1 Token 成本与延迟测算

以一篇 **5000 词**的典型竞赛论文为基准：

| Agent | 输入 Token | 输出 Token | 输入内容 |
|-------|-----------|-----------|----------|
| 1. Rubric Parser | 3K | 1K | 仅 rubric 文本（不传全文） |
| 2. Structure & Logic | 8K | 2K | 全文 + 结构 schema |
| 3. Argument & Evidence | 8K | 3K | 全文 + rubric 维度 |
| 4. Writing Style & Voice | 8K | 1.5K | 全文 |
| 5. Citation & Format | 8K | 1.5K | 全文 + ref 列表 |
| 6. Originality Guardian | 10K | 1K | 全文 + 比对片段 |
| 7. Language Polishing | 8K | 3K | 全文 |

| 汇总 | 数值 |
|------|------|
| 总输入 Token | ≈ 53K |
| 总输出 Token | ≈ 13K |
| **单次审查总消耗** | **≈ 66K tokens** |
| 按 GPT-4o 价格估算 | **≈ $0.26/篇（约 ¥1.90）** |
| 月均审查 100 篇 | ≈ ¥190 |
| 年化成本 | ≈ ¥2,280 |

**结论：Token 成本在日常运营量级下几乎可忽略。真正的优化价值不在于省钱，而在于减少端到端延迟、降低工程复杂度、提高单次响应的可靠性和可调试性。**

> **关于 Calibration Engine**：校准时不调用 LLM，纯统计计算，无额外 token 成本。

#### 2.6.2 Agent 合并可行性矩阵

| Agent X | 与 Agent Y 合并 | 判定 | 理由 |
|---------|----------------|------|------|
| A1 Rubric | A2/A3/A4/A5/A6/A7 | ❌ 不可 | Rubric 解析是前置任务，本质是"配置层"而非"审查层" |
| A2 Structure | A3 Argument | ⚠️ 可但存疑 | 结构看"骨架有没有"，论证看"肉好不好"。合并后 prompt 臃肿 |
| A2 Structure | A4 Style | ❌ 不可 | 宏微观分离是设计根基 |
| A3 Argument | A4 Style | ❌ 不可 | 审逻辑和审表达混在一起会互相污染 |
| **A4 Style** | **A7 Language** | **✅ 可合并** | 风格和语言是连续谱。代价：A7 改写权限需严格约束 |
| **A5 Citation** | **A6 Originality** | **✅ 可合并** | 同属"学术诚信"范畴。代价：实现方式不同（规则 vs 语义） |

**可行合并点**：
- **合并点①**：Agent 4 + Agent 7 → 统管"语言表达层"
- **合并点②**：Agent 5 + Agent 6 → 统管"学术诚信层"

#### 2.6.3 方案对比总览

```
                       保守 ←────────────────→ 激进

 7 Agent ────── 5 Agent ⭐ ────── 4 Agent ────── 3 Agent
 (v1.1)        (推荐)            (可选)         (不推荐)

 代价/篇：    $0.26          $0.22          $0.20          $0.19
 延迟：       17s            13s            10s            8s
 质量：       基准           基本无损失      有轻微损失      显著损失
 调试：       简单           简单           中等           困难
```

---

### 2.7 决策备忘：7 Agent vs 5 Agent 终极对比

> **本章为 v1.4 新增。** 基于六维度量化分析，给出明确的开发决策建议。

#### 2.7.1 架构速览

```
═══════════════════════════════════════════════════════════════
  方案 A：7 Agent（原版）                方案 B：5 Agent（精简）
═══════════════════════════════════════════════════════════════

  A1  Rubric Parser          ◀───▶   A1  Rubric Parser
  A2  Structure & Logic      ◀───▶   A2  Structure & Logic
  A3  Argument & Evidence    ◀───▶   A3  Argument & Evidence
  A4  Writing Style & Voice  ┐
                             ├───▶   A4  Language & Style
  A7  Language Polishing     ┘
  A5  Citation & Format      ┐
                             ├───▶   A5  Academic Integrity
  A6  Originality Guardian   ┘
═══════════════════════════════════════════════════════════════
  3 轮调度（R1→R2→R3）              2 轮调度（R1→R2）
  7 次 LLM 调用                       5 次 LLM 调用
═══════════════════════════════════════════════════════════════
```

#### 2.7.2 量化指标对比

| 维度 | 7 Agent | 5 Agent | 差值 | 判决 |
|------|---------|---------|------|------|
| **每篇 Token 消耗** | ≈ 66K | ≈ 58K | -12% | 差值不到 ¥0.30，**无实际意义** |
| **每篇成本** | ≈ $0.26 (¥1.90) | ≈ $0.22 (¥1.60) | -¥0.30 | 月审 100 篇仅差 ¥30 |
| **端到端延迟** | ≈ 17s | ≈ 13s | -4s | 对异步批处理场景**无感知差异** |
| **LLM 调用次数** | 7 次 | 5 次 | -2 次 | 减少 API 故障面，**有意义** |
| **调度轮次** | 3 轮 | 2 轮 | -1 轮 | 减少一轮串行等待，**有意义** |

**结论：Token 成本差异可忽略。7→5 的真正价值不在省钱，在工程层面。**

#### 2.7.3 审查质量对比

| 审查维度 | 7 Agent 粒度 | 5 Agent 粒度 | 差异 |
|----------|-------------|-------------|------|
| **评分标准解析** | 独立 Agent，专注 | 独立 Agent，专注 | 无 |
| **结构骨架** | 独立 Agent，专注 | 独立 Agent，专注 | 无 |
| **论证深度** | 独立 Agent，专注 | 独立 Agent，专注 | 无 |
| **写作风格** | 独立 Agent | 合并到 A4 | ⚠️ 有讨论空间 |
| **语言润色** | 独立 Agent（唯一可改写） | 合并到 A4 | ⚠️ 有讨论空间 |
| **引用完整性** | 独立 Agent | 合并到 A5 | ⚠️ 有讨论空间 |
| **原创性/AI痕迹** | 独立 Agent | 合并到 A5 | ⚠️ 有讨论空间 |

#### 2.7.4 合并点①深度分析：A4 (Style) + A7 (Language)

```
优势：
✅ 两个维度在语言表达这条连续谱上自然衔接
   语法错误 → 表达不当 → 风格问题，没有硬边界，一个 Agent 能整体把握
✅ 减少一次全文传输（8K token 省了）
✅ 学生看到的反馈更连贯：不用在"风格问题"和"语法问题"之间来回跳

劣势/风险：
⚠️ A7 在你的设计中是唯一有「改写权限」的 Agent
   合并后需要严格约束：语法修正可改写；风格建议仅批注
   这条分界线必须在 prompt 里用铁律写死
⚠️ 如果 prompt 没写好，容易出现风格建议侵入式改写
⚠️ 学生主权的最重要防线在合并后变薄了

缓解措施：在 A4 的 prompt 里显式定义"改写权限边界"
  · 允许改写：拼写、语法、标点、主谓一致
  · 仅批注（不可改写）：句式选择、语态分布、用词偏好、段落节奏
  · 输出格式要求：改写内容用 [REWRITE] 标记，批注用 [SUGGEST] 标记
```

#### 2.7.5 合并点②深度分析：A5 (Citation) + A6 (Originality)

```
优势：
✅ 概念内聚度高：引用缺失 + 抄袭 + AI痕迹 = 学术诚信的不同表现面
   现实中这三者经常联动：
   · AI 生成引用 → 既是 citation 问题，也是 originality 问题
   · 大段未标注引用 → 既是 citation 问题，也是抄袭
   一个 Agent 能交叉发现这类复合问题
✅ 减少一次全文传输（8K-10K token 省了）

劣势/风险：
⚠️ 实现路径不同：
   · 引用检查 → 规则引擎（cite↔ref 配对、格式正则匹配）
   · 原创性检查 → 语义比对（向量相似度、AI痕迹识别）
   一个 prompt 管两种逻辑 → prompt 复杂度上升
⚠️ 但比合并 A2+A3 的"结构+论证"合并要安全得多

缓解措施：在 A5 的 prompt 里设两条独立的内部子任务
  · 子任务 5a：引用完整性（规则驱动，输出结构化检查表）
  · 子任务 5b：原创性审查（语义比对驱动，输出相似度和可疑标记）
```

#### 2.7.6 不会合并的核心三 Agent

无论 7 还是 5，以下三个 Agent 从不合并：

| Agent | 不合并的根本原因 |
|-------|-----------------|
| **A1 Rubric** | 它是配置层，不是审查层。生命周期不同，缓存后一笔复用 |
| **A2 Structure** | "先看骨架、后看血肉"是设计哲学根基。结构是二元判断（有/无），论证是连续评估（好/坏），混在一起会互相污染 |
| **A3 Argument** | 它是 L2 赛事定制的核心载体，也是 Calibration Engine 校准的重中之重。独立意味着调校时只改这一个，不影响其他 Agent |

#### 2.7.7 工程维度对比

| 维度 | 7 Agent | 5 Agent | 分析 |
|------|---------|---------|------|
| **首次开发量** | 高（写 7 套 prompt + 7 个调度逻辑） | 中（写 5 套，其中 2 套更复杂） | 5 Agent 单 prompt 更重，总开发量差约 20% |
| **Prompt 单条复杂度** | 低（每个职责单一，prompt 精悍） | 中（A4 和 A5 的 prompt 需要分内部子任务） | 7 版本更容易写出质量 |
| **调试** | 简单（问题定位精确到单一维度） | 简单到中等（A4/A5 内部需子任务定位） | 微幅差距 |
| **版本迭代** | 灵活（改风格审查不影响语言润色） | 一般（改 A4 影响两个维度，需要回归测试） | 7 版本迭代更安全 |
| **L0/L1/L2 分层** | 清晰 | 基本不受影响 | 两个合并点都在 L0/L1 层 |
| **Calibration Engine 兼容** | 完全兼容 | 完全兼容（校准对象是配置文件不是 Agent） | 无差异 |
| **扩展新赛事类型** | 需要改动 A3 + A4 | 需要改动 A3 + A4 | 无差异 |

#### 2.7.8 运营维度对比

| 维度 | 7 Agent | 5 Agent | 分析 |
|------|---------|---------|------|
| **反馈报告结构** | 7 维度评分，颗粒度最细 | 5 维度评分，合并后维度略粗 | 7 版本给学生/导师的"诊断面板"更精确 |
| **学生能看出问题归属吗** | ✅ 语法问题和风格问题分开列 | ⚠️ 合并列在"语言与表达"下，但内部可标记来源 | 需要确保报告内部区分 |
| **导师覆盖 AI 建议** | 逐 Agent 覆盖，精准 | 合并 Agent 内覆盖，需子任务级粒度 | 微幅差异 |
| **API 故障容错** | 7 次调用 → 7 个潜在故障点 | 5 次调用 → 降低 28% 故障面 | 5 版本更稳 |

#### 2.7.9 开发路线对比

```
═══════════════════════════════════════════════════════════════
路线 1（v1.2/v1.3 原推荐）：先开发 7 → 后合并为 5
───────────────────────────────────────────────────────────────
开发 7 Agent：    ████████████████████████████████  (100%)
合并 A4+A7：      ████████                        (+20% 重构)
合并 A5+A6：      ████████                        (+20% 重构)
───────────────────────────────────────────────────────────────
总投入：          140%

路线 2（v1.4 新推荐）：直接开发 5 Agent，一步到位
───────────────────────────────────────────────────────────────
直接开发 5 Agent： ██████████████████████████████  (90%)
───────────────────────────────────────────────────────────────
总投入：          90%

结论：如果你最终目标是 5 Agent，直接开发 5 比分两步走节省 ~36% 总投入。
═══════════════════════════════════════════════════════════════
```

> 但路线 1 有一个不可替代的价值：在 7 Agent 阶段亲眼看到 A4 和 A7 各自产出的独立报告，用真实数据验证「风格问题」和「语法问题」的占比和重叠率，再做出合并决策。这是一个"先验证再优化"的保守路线。

#### 2.7.10 决策矩阵

**你应该选择 7 Agent 如果：**

| 条件 | 权重 |
|------|------|
| 你对质量的要求 > 对速度的要求（辅导机构的精度是护城河） | 🔴 最高 |
| 你希望反馈报告给学生的「诊断面板」维度最细（7 个维度 vs 5 个） | 🟡 中 |
| 你愿意为更精准的问题定位多写 2 个 Agent 和 1 轮调度 | 🟡 中 |
| 你手上有多个竞赛类型要做、需要先跑通最稳健的架构 | 🔵 参考 |
| 你认为「先验证再合并」的路线比「一步到位」更安全 | 🔵 参考 |

**你应该选择 5 Agent 如果：**

| 条件 | 权重 |
|------|------|
| 你的开发资源有限，需要尽快上线可用版本 | 🔴 最高 |
| 你认为语法/风格合在一起不影响反馈质量（实践中确实边界模糊） | 🟡 中 |
| 你认为引用/原创合在一起能协同发现复合问题（引用缺失+疑似抄袭） | 🟡 中 |
| 你愿意接受多花一点 prompt 工程复杂度（A4/A5 的内部子任务设计） | 🔵 参考 |
| 你需要更少的 API 调用次数来降低模型故障面 | 🔵 参考 |

#### 2.7.11 v1.4 最终建议

```
╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║   直接开发 5 Agent，但 A4 和 A5 内部保留子任务边界。          ║
║                                                              ║
║   理由：                                                      ║
║   1. Token 和延迟差异可忽略，真正决定效率的是工程复杂度        ║
║   2. 合并的两个点（A4+A7, A5+A6）在各个设计文档中都标记为     ║
║      "✅ 可合并"，不是勉强合并——它们在概念上是自然内聚的       ║
║   3. 节省 2 次 API 调用 = 减少 28% 的外部服务故障风险         ║
║   4. 分两步走的总成本是 140%，一步到位是 90%                  ║
║   5. 以后如果发现需要拆分，只需 1 个 Agent 的拆分量，         ║
║      远比合并 2 个 Agent 容易                                 ║
║                                                              ║
║   唯一前提：A4 的 prompt 里必须用铁律划清"改写权限边界"        ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝
```

**一句话总结：**

> 7 Agent 是「规格最高的方案」，5 Agent 是「性价比最高的方案」。在辅导机构场景下，月均 100 篇的量级 + 对反馈精度的要求，**5 Agent 在"够用"和"够精"之间取了最优解**。省下的 2 个 Agent 不是质量妥协——是让两个天然内聚的职责合到同一个 context window 里，反而可能产生更好的协同效果。

---

## 三、工作流设计

### 3.1 审查工作流：五阶段管道（在线）

```
输入阶段          审查阶段                        反馈阶段        迭代阶段
┌──────┐    ┌─────────────────────────┐    ┌──────────┐    ┌──────────┐
│ Stage1│───▶│ Stage2: Multi-Agent Review│───▶│ Stage3:  │───▶│ Stage4:  │
│ Intake│    │  Rubric→Structure→Logic    │    │ Feedback │    │ Revision │
│       │    │  →Evidence→Style→Citation  │    │ Report   │    │ Support  │
└──────┘    └─────────────────────────┘    └──────────┘    └──────────┘
                                                               │
                                                          ┌────▼─────┐
                                                          │ Stage5:  │
                                                          │ Final QC │
                                                          │ & Export │
                                                          └──────────┘
```

#### Stage 1: 材料摄入与预处理（Intake）

| 类别 | 材料 | 必须/可选 | 说明 |
|------|------|-----------|------|
| **学生文稿** | 完整英文稿件（.docx / .tex / .md） | ✅ 必须 | 支持带格式文档 |
| **竞赛信息** | 竞赛名称、类别、提交截止日期 | ✅ 必须 | 用于加载对应配置文件 |
| **评分标准** | 官方 rubric / judging criteria | ✅ 必须 | 有官方原文最佳；无可使用系统推断版本 |
| **字数要求** | 最低/最高字数限制 | 🟡 建议 | |
| **格式要求** | 字体、行距、页边距等 | 🟡 建议 | |
| **往年范文** | 该竞赛往届获奖作品 | 🔵 可选 | 用于风格对标和质量基准 |

#### Stage 2: 多 Agent 并行审查（5 Agent 设计）

```
Orchestrator 调度顺序:

Round 1（宏观 + 语言）- 并行执行:
  ├── Agent 1: Rubric Parser → 读取 rubric_calibrated.json → 生成检查清单
  ├── Agent 2: Structure & Logic → 读取 structure_schema_calibrated.json
  ├── Agent 3: Argument & Evidence → 读取 evidence_patterns.json
  └── Agent 4: Language & Style → 读取 style_guide.json → 语法修正 + 风格审查

Round 2（诚信）- 基于 Round 1 结果执行:
  └── Agent 5: Academic Integrity → 引用完整性 + 原创性检测
```

#### Stage 3-5

反馈报告生成、学生修改辅助、终审与导出。

---

### 3.2 校准工作流：赛季周期（离线）

```
┌──────────────────────────────────────────────────────────┐
│                    赛季校准周期                            │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  赛季前（赛前 2-3 个月）                                    │
│  ├── 上传/更新官方 rubric PDF                            │
│  ├── 将新收集的获奖/失败文章入库                            │
│  ├── 运行 Calibration Engine                            │
│  ├── 审核校准建议 → 锁定本赛季配置文件                      │
│  └── 产出：配置文件版本快照                                │
│                                                          │
│  赛季中（辅导+提交阶段）                                    │
│  ├── 审查管线使用锁定版本配置                               │
│  ├── ❌ 不修改配置文件（避免赛季中途干扰）                    │
│  └── 收集本届学生文稿（待赛后分析）                          │
│                                                          │
│  赛季后（结果公布后 2 周内）                                 │
│  ├── 标注本届获奖/失败文章                                  │
│  ├── 收集新公开的外部获奖文章                                │
│  ├── 运行 Calibration Engine（增量模式）                  │
│  ├── 审核校准建议 → 产出新配置文件草案                       │
│  ├── 记录赛季校准日志（correlation、调整项、样本量）          │
│  └── 产出：校准后的配置文件 + 校准日志                      │
│                                                          │
│  质量指标：系统预测分数与实际获奖排名的 Spearman ρ            │
│  ρ > 0.8 → 配置锁定；ρ 0.6-0.8 → 微调；ρ < 0.6 → 大修    │
└──────────────────────────────────────────────────────────┘
```

---

### 3.3 操作手册：赛季校准的具体动作

#### 3.3.1 你需要做的（人的操作）

```
赛季前校准窗口：

  Step 1 ─ 上传或确认官方 rubric
              动作：把官方 PDF 或文本放入对应竞赛目录
              耗时：5 分钟（有官方文件）或跳过（无官方文件时用推断版）

  Step 2 ─ 标记样本集
              动作